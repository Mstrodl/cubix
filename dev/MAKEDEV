shell.run("mkdir /proc")
shell.run("mkdir /bin")
shell.run("mkdir /sbin")
shell.run("mkdir /boot")
shell.run("mkdir /etc")
shell.run("mkdir /home")
shell.run("mkdir /lib")
shell.run("mkdir /mnt")
shell.run("mkdir /root")
shell.run("mkdir /usr")
shell.run("mkdir /var")
shell.run("mkdir /tmp")
shell.run("mkdir /media")
