On the Subject of Login Manager

Login in cubix is made by the login() function in /lib/login_manager. login() reads /etc/shadow, a file that stores users, their password and the salt
By now, passwords are stored in the sha256(password + salt) ["man hashmngr"] form, and the defualt home folder for a user is /home/<user>
