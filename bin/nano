#!/usr/bin/env lua
--/bin/nano: an alternative to program a good text editor.

function main(args)
    --calls edit, but, one time, i might be able to program a full editor like nano.
    os.runfile_proc("/rom/programs/edit", {os.cshell.resolve(args[1])})
end

main({...})
