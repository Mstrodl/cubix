#!/usr/bin/env lua
--/bin/sleep

_handler = {}
_handler.signal_handler = function (sig)
    if sig == 'kill' then
        --print("sleep: recieved SIGKILL")
        return 0
    end
end

function main(args)
    local t = args[1]
    os.sleep(tonumber(t))
end
