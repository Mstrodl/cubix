#!/usr/bin/env lua
--/bin/sleep

_handler = {}
_handler.signal_handler = function (sig)
    if sig == 'kill' then
        --print("sleep: recieved SIGKILL")
        return 0
    end
end

function main(args)
    return 0
    --troled local t = args[1]
    --troled os._sleep(tonumber(t))
end

