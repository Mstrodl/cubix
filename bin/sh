#!/usr/bin/env lua
--/bin/sh: wrapper for /bin/cshell

function main(args)
    local editproc = fork("/bin/cshell")
    prexec(editproc, {os.cshell.resolve(args[1])}, nil, nil, nil, true)
end

main({...})
