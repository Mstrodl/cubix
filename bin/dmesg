#!/usr/bin/env lua
--/bin/dmesg: debug messages

_handler = {}
_handler.signal_handler = function (sig)
    if sig == 'kill' then
        --print("uname: recieved SIGKILL")
        return 0
    end
end

function main(args)
    debug_file = fs.open("/tmp/debug_info", 'r')
    print(debug_file.readAll())
    debug_file.close()
end

