#!/usr/bin/env lua
--/bin/su: logins to root

function main(args)
    libproc.chair_register(pget_running())
    local loginproc = fork("/sbin/login")
    prexec(loginproc, {"root"}, nil, nil, nil, true)
end

main({...})
