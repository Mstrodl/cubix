#!/usr/bin/env lua
--/bin/clear

_handler = {}
_handler.signal_handler = function (sig)
    if sig == 'kill' then
        --print("clear: recieved SIGKILL")
        return 0
    end
end

function main(args)
    os.runfile("clear")
end

